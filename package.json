{
  "name": "feedsense",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:debug": "FEEDSENSE_DEBUG=1 node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test tests/**/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
